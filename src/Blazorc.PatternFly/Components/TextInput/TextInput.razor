@inherits Blazorc.PatternFly.Components.TextInput.TextInputComponent

<input aria-label="@Label" 
       class="pf-c-form-control" type="@Type"
       aria-invalid="@IsInvalid.ToString().ToLower()"
       aria-required="@IsRequired"
       bind="@Value"
       readonly="@IsReadOnly"
       disabled="@IsDisabled"
       />
