@inherits Blazorc.PatternFly.Components.Icon.DynamicIconComponent

@CustomRender

@functions
{
    private RenderFragment CustomRender;

    private RenderFragment CreateComponent() => builder =>
    {
        builder.OpenComponent(0, Type.GetType($"Blazorc.PatternFly.Components.Icon.{Icon}Icon"));

        builder.AddAttribute(1, "Class", BindMethods.GetValue(Class));

        builder.CloseComponent();
    };

    protected override void OnInit()
    {
        base.OnInit();

        CustomRender = CreateComponent();
    }
}